pub(crate) struct Checkpoint {
    // load / save for models
    // save config.json next to so hypers are reproducible
    // maybe generic over the actor so we can just do this?
    // not sure how burn saves loads best
}
