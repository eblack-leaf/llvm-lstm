// Start with background about LLVM
// What passes are + Why passes matter + Pass ordering problem
// model vs fixed pipeline passes of 03
// previous work referenced (Compiler Gym), but we do speedup not IR reduction
// our 38 funcs as pool + how we will explore data first -> collect random pass sequences + bench + analyze ( we have figures from previous work that are not present here where we collected ~1 million samples 25000 for the 38 benches to see what happens at them. i no longer have that code here, but i have some figures about it and should reference it as starting point)
// motivation for selecting 6 to focus on based on EDA metrics (the 6 in benchmarks dir were pulled from pool for beating 03 capability + good range of learning)
// we have concerns about collecting in parallel as timing is precise and doing many at once is only way to reasonably collect this many runs, so we make bench-noise path which does solo vs 16 rayon threads and compares difference (measurements are )
// how we structured the IR at first (18 dim of counts non-ordered but they were too similar and it lacked order info) vs now (chunk deltas of frequency to have order)[we really just changed it until the vectors were distinguished enough cosine similarity but its still kinda close]
// now that we have IR differences to condition on, we try to get PPO returns + adv settled. We start with naive episode return + baseline adv with auto-tfx + auto-gru
// we see from that how it promotes all steps with terminal even though it might not have affected it right way so learning is scattered and EV is random (we still need to run this experiment now, just know it will probably do that based on math and previous runs before this formal experiment run)
// then we try weighted by how much IR changes with weighted return on both those arches (will add comparison after we see how it does)
// we try predictor which uses the collected bench-cache to generate dataset that ties step to terminal speedup as value predictor so it can estimate the return for partial steps from the ground truth of what actually benching that run does
// mention about training rmse and huber loss reasoning? im not sure about that, but also reference dataset.png to show whats the landscape of our training as well, bench-cache just happens to have good uses as training data + exploration of whats happening during training
// we try that but it hasnt shown best correlation (from test runs, still need to make formal run for this, but im sure it will not be that good)
// we try parallel models (seq, conclave) too see if the model can resemble the compiler and compare passes when deciding all at once (we need to do a few runs with this but since only episode return can work as they are not auto and dont have steps to attribute, also did not see much gains when i did test runs on this)
// since the IR reduction has been done before, we try that out since its easy to adapt our pipeline to it, and see what happens (will do run to have results later, but EV in these gets near 1, so our pipeline works but IR is not our real goal)
// we finally see that maybe we cant value the steps, too many no-ops, too little reward possibility as most runs are negative, hard correlations between attribution of credit to steps, so we explore GRPO to avoid the value prop all together
// talk about runs there, + successes (we have that run already and its good)
// we then evaluate the grpo one as it showed best results (we have eval command which tests against random, greedy, best of sample, ...)
